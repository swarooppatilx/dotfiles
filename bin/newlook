#!/bin/sh
wal -c

# pick a random image from folder
DIR="/home/daenerys/pics/walls"
PIC=$(find "$DIR" -type f | shuf -n 1)

# generate and set color scheme
wal -i "$PIC"

# refresh dmenu / other dependent tools
xdotool key super+F5
